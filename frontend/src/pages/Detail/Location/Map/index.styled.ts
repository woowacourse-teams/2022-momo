import styled from '@emotion/styled';

const MapWrapper = styled.div`
  display: flex;
  justify-content: center;

  width: 100%;
`;

const Map = styled.div`
  width: 90%;
  aspect-ratio: 1 / 1;

  border-radius: 10px;
`;

export { MapWrapper, Map };
