import Loading from '.';

const story = {
  title: 'Component/Loading',
  component: Loading,
};

export default story;

function Template() {
  return <Loading />;
}

export const Default = Template.bind({});
