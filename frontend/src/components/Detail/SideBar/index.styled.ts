import styled from '@emotion/styled';

const Container = styled.div`
  display: flex;
  flex-direction: column;
  gap: 3rem;

  position: sticky;
  top: 6rem;

  height: fit-content;
`;

export { Container };
